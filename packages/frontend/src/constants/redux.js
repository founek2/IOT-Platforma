import keymirror from 'key-mirror';

export const ActionTypes = keymirror({
	ADD_DEVICE: null,
	REMOVE_DEVICE: null,
	SET_DEVICES: null,
	UPDATE_DEVICE: null,
	SET_USER_NAMES: null,
	UPDATE_STATE_DEVICE: null,
	UPDATE_DEVICES: null,
});