{
    "name": "framework-ui",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "@date-io/date-fns": "^1.x",
        "@material-ui/pickers": "^3.3.10",
        "date-fns": "^2.16.1",
        "ramda": "^0.27.1",
        "winston": "^3.3.3"
    },
    "main": "lib/index.js",
    "typings": "index.d.ts",
    "scripts": {
        "build": "rimraf tsconfig.tsbuildinfo lib && tsc -p . && yarn copy-files",
        "copy-files": "copyfiles -u 1 src/**/*.html src/**/*.css lib/",
        "dev": "tsc -w",
        "test": "jest",
        "coverage": "yarn test --collectCoverage"
    },
    "devDependencies": {
        "@types/react-redux": "^7.1.12",
        "@types/ramda": "0.27.34",
        "copyfiles": "^2.4.1",
        "typescript": "^3.9.6"
    }
}