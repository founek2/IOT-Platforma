{
    "name": "iot-platforma",
    "license": "GPL-3.0-only",
    "author": "Martin Skalický",
    "private": true,
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "build": "lerna run --parallel --scope backend --scope frontend --scope backend-mqtt build",
        "clean": "lerna run --parallel clean",
        "test": "lerna run --scope backend --scope backend-mqtt --scope framework-ui test",
        "watch": "lerna run --parallel --scope common --scope framework-ui dev",
        "prepare": "lerna run --scope framework-ui build && lerna run  --scope common  build",
        "dev:be": "yarn workspace backend dev",
        "dev:fe": "yarn workspace frontend dev",
        "dev:be-mqtt": "yarn workspace backend-mqtt dev"
    },
    "dependencies": {},
    "devDependencies": {
        "@types/node": "^16.11.7",
        "lerna": "^3.22.1",
        "rimraf": "^3.0.2",
        "typescript": "^3.9.6"
    },
    "workspaces": [
        "packages/*"
    ]
}
